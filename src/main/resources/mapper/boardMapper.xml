<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo3.dao.BoardDAO">
    <select id="list" resultType="com.example.demo3.vo.BoardVO">
        select
            board_no as boardNo,
            user_id as userId,
            board_title as boardTitle,
            board_contents as boradContents
        from board
    </select>

    <insert id="insert">
        insert into board (boardNo, user_id, board_title, board_content)
        values (#{boardNo}, #{userId}, #{boardTitle}, #{boardContent})
    </insert>

</mapper>